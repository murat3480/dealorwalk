/**
 * Future home for DealorWalk **comment summary** AI: friend / community opinion
 * rollup for shared deals (aggregate verdicts, themes, suggested actions).
 * Separate from `src/lib/commentSummary.ts` (existing helpers)—placeholder only.
 */

export async function summarizeDealComments(input: unknown) {
  void input
  return null
}
